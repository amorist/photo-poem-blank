---
name: photo-poem-blank
description: 把任意一张照片做成"填空小诗"竖版视频——先读懂照片里的具体细节和情绪，写一首小诗，再把 5–7 个局部从照片里抠出来飞进诗句的空格里。Use when 用户想用照片生成小诗视频、图文填空/剪影填空、把旅行照变成有诗意的短视频，或提到 photo poem、填空诗、照片配小诗。
---

# 照片填空诗

一张照片 + 一首小诗，诗的每个空由照片里抠下来的一小块填上；那块原本的位置留一个半透明取景框，照片始终完整可见。输出 1080×1440 竖版视频（含配乐与音效）、封面图、成品静帧。

## 工作流

### 1. 先读照片，再写字

用图像工具**真的看一眼**这张照片，然后列出 6–8 个候选细节，每个写清：它是什么（一个具体名词）、在画面哪个位置（像素框）、在 55px 高时还认不认得出、什么颜色。读法见 [references/poem-writing.md](references/poem-writing.md)。

### 2. 选出 5–7 块切片

只保留满足全部条件的：能被一个名词命名（花、水、手、眼、灯、栏杆…）、缩小到 55px 高仍一眼可辨、材质/尺度彼此不同（别选两块都是石头）、四边离照片边缘至少 15px、彼此不重叠、不把人的脸切一半。

拿不准就先出候选图看：

```bash
python3 scripts/inspect_photo.py photo.jpg --grid                    # 带坐标网格的取景图
python3 scripts/inspect_photo.py photo.jpg --boxes "花:690,302,92,60;牌:100,62,150,70"
```

第二条命令会把候选切片按**真实入诗尺寸**×3 像素排成对比图——用它判断"够不够清楚"，不要凭想象。

### 3. 写诗

3–4 行，正文 24–42 字，第二人称、现在时、语气克制温暖，最后一行留一个轻轻的转折。硬规则：

- **图片即词**：每个空格由切片承担，正文里不要再写同一个词。切片是「眼」就写 `把{5}里的整个春天`，不要再出现「眼」字。
- 每个空填进去后整句要读得通，且必须是具体的、照片里真有的东西。
- 不写品牌、政治、证件信息；不写照片里看不出来的事。
- 用 `{0} {1} …` 占位，脚本会自动补全角括号并留内间距。

### 4. 生成

复制 `scripts/example_config.json` 改成本片的配置（图片路径、`photo_crop` 可留空自动取景、`lines`、`slices`），然后：

```bash
python3 scripts/poem_video.py build config.json
python3 scripts/poem_video.py stills config.json --times 2.6,9.6   # 需要单独查画面时
python3 scripts/selftest.py                                        # 换环境/改脚本后先跑一遍
```

build 会打印版式体检（每行宽度、每块切片尺寸、原位取景框坐标、总时长）。以下情况都会以 `!` 报出来，改配置再跑：行宽超出 40–95%、切片越界 / 互相重叠 / 两块用了同一个框、切片被放大超过 1.3×（源图局部比入诗尺寸小，会糊）、`slice_gap` 短于 `pick+fly+settle`（会破坏"一块落稳再走下一块"）、封面定格比切片开始还晚（无封面版的裁剪会变负数）。

渲染不落盘：每帧合成后直接以原始 RGB 走管道喂给两条 ffmpeg（cover / no-cover 并行编码，音频一步混流），6 块切片的默认时长构建约 5 秒。需要逐帧留档时加 `--dump-frames DIR`。

`selftest.py` 会造出不同比例/尺寸/格式的照片（横、竖、方、全景、黑白、透明 PNG、480×320 到 3600×2400）跑完整管线，并逐项验证产物：必须是带音轨的 1080×1440 视频、响度 −14 LUFS、音乐 stem 有调性、音效 stem 落在每个切片节奏点上、成片音轨与合成的乐谱一致（相关系数 >0.9）。加 `--full` 用正式时间轴。

### 5. 交付前自己看一眼

看 `*-cover-1080x1440.jpg`（就是成品画面）和一张飞行中的静帧：切片在句子里是否与文字同一中线、取景框位置是否对应、诗句读起来是否顺。确认后交付 3 个文件：主片（带封面定格）、无封面版、封面图（同一张图既是封面也是成品静帧）。

## 不能动的默认

这些是用户确认过的效果，除非明确要求改：

- 上半 48% 暖白 `#F1F1EF` + 下半 52% 全出血照片，中间不加分隔线。
- 正文黑色中性无衬线（Hiragino Sans GB W3），字号 3.2% 画布宽，行距 1.88，每块切片 1.6×正文字高、2–4px 圆角、无描边无阴影，全角括号包住。
- 原位留**半透明**取景框（填充 95/255、描边 200/255），不要用实心矩形把照片挖掉。
- 动效：拾起 → 弧线飞行（方向拉伸 + 拖影 + 侧倾）→ 落位回弹；**一块完全落稳再走下一块**，中间留 0.1 秒呼吸。
- 第一帧必须是成品画面（0.8s 定格 + 0.25s 叠化进入正片）——否则平台会把白板当封面。
- 音乐/音效由 timeline 推导，落位音正好是上行五声音阶的一个音；成片统一到 −14 LUFS。

细节参数、可调项与设计理由见 [references/visual-spec.md](references/visual-spec.md)；配乐结构见 [references/audio-spec.md](references/audio-spec.md)。

## 依赖

`python3`（Pillow、numpy、scipy）与 `ffmpeg`。字体自动在系统里找中文字体，找不到就在配置里指定 `font.path`。

读图会自动处理 EXIF 旋转、透明通道（合成到白底），并在 Pillow 读不了时回退到 `sips`（macOS）/`ffmpeg` 转码，因此 iPhone 的 HEIC、AVIF 等也能直接喂进来。
